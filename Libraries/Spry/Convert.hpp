#ifndef SPRY_CONVERT_HPP
#define SPRY_CONVERT_HPP

namespace Spry
{
	class Convert
	{
	public:
		template<typename T, typename From>
		static T To(const From& from)
		{
			int a[-1];
		}

		template<>
		static int To(const char* from)
		{
			
		}

	private:
		Convert();
		~Convert();
	};
}

#endif